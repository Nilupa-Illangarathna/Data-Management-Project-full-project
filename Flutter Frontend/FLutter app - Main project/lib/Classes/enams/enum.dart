enum CustomPlaceType {all, cafe, restaurant, club, bar, hotel }

// Model Bottom Sheet Flags
// Homescreen
enum HomeScreenMBSFlags { shopdetails, seemore }

//for API response
enum ApiResponse {
  Success,
  Failed,
  SERVER_ERROR,
  CREATED,
  BAD_REQUEST,
  UNAUTHORIZED,
  FORBIDDEN,
  NOT_FOUND,
}

enum OtpTypes { EMAIL, MOBILE }
